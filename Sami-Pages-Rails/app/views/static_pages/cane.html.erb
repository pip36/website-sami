<% @title = "Cane" %>

<div class="container">
  <h1 style="margin-top: 60px"> Cane <h1>
<p> Here can go some information and pictures of cane. <p>
<div class="row">
<% @products.each do |product| %>



<%= form_for @order_item, url: order_items_url, remote: true do |f| %>
  <%= f.hidden_field :product_id, value: product.id %>
  <%= f.hidden_field :cart_id, value: @cart %>

<div class="col-md-6">
<div class="product-card">
  <h3> <%=product.name%> </h3>
  <br>
  <p> <%=product.description%> </p>
  <br>
  <p><%=number_to_currency(product.unit_price, unit:'£')%> per piece<p>
  <%= f.select :quantity, ['0','5','10','15','20','25'] %>
  <%= f.submit "add to cart" %>
</div>
</div>
    <% end %>

  <% end %>
</div>
</div>
